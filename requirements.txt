# All runtime dependencies live in pyproject.toml. This file is a
# convenience shim for the `pip install -r requirements.txt` muscle
# memory — it just installs the project itself, which pulls every
# declared dependency.
#
# For development extras (pytest, ruff), use:
#     pip install -e ".[dev]"
-e .
